# Kept in sync by hand with the [project.dependencies] list in pyproject.toml.
# This file exists only because Streamlit Community Cloud reads requirements.txt
# from the repo root; pyproject.toml is the source of truth for local dev.
# When you add or change a dependency, update both.
python-chess>=1.999
anthropic>=0.40
voyageai>=0.3
psycopg2-binary>=2.9
sqlalchemy>=2.0
pgvector>=0.3
streamlit>=1.38
python-dotenv>=1.0
pydantic>=2.8
httpx>=0.27
beautifulsoup4>=4.12
