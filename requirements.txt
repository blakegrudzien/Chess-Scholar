# Kept in sync by hand with the [project.dependencies] list in pyproject.toml.
# This file exists only because Streamlit Community Cloud reads requirements.txt
# from the repo root; pyproject.toml is the source of truth for local dev.
# When you add or change a dependency, update both.
python-chess>=1.999
anthropic>=0.40
voyageai>=0.3
psycopg2-binary>=2.9
# sqlalchemy/pydantic: not currently imported anywhere -- kept for a planned
# future ingestion/file-processing pass, not leftover cruft (see pyproject.toml).
sqlalchemy>=2.0
pgvector>=0.3
# Capped: see pyproject.toml's comment on this same line -- src/ui/styles.py
# targets an unversioned Streamlit-internal CSS class name that a future
# release could rename with no error, only a silent visual regression.
streamlit>=1.38,<1.60
python-dotenv>=1.0
pydantic>=2.8
httpx>=0.27
beautifulsoup4>=4.12
scikit-learn>=1.4
joblib>=1.3
